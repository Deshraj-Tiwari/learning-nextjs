import React from "react";

const serverComponent = () => {
  return (
    <span className="text-purple-600 text-3xl font-bold">
      Any component is server component by default in NEXT js and you can use it
      to do sever side rendering, Static site generation and stuff like that.
    </span>
  );
};

export default serverComponent;
