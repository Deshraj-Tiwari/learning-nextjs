import React from "react";

const NotFound = () => {
  return (
    <span className="text-green-600 text-3xl font-bold">
      Create your own PageNotFound page. <br />
      <br />
      Always remember to keep the not found file directly in the app directory
      and name it not-found
    </span>
  );
};

export default NotFound;
